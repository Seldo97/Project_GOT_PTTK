{% extends 'baseTemplate.twig' %}

{% block main %}

    Strona Główna, Witaj

    {% if 'ROLE_administrator' in LoggedUser.getAuthorities %}
        {{ LoggedUser.getAuthorities }} {{ LoggedUser.getName }}
    {% endif %}

{% endblock %}